const ENVIRONMENT = {
  production: 'production',
  staging: 'staging',
  development: 'development',
};

const ROLES = {
  admin: 'Admin',
  user: 'User',
};

module.exports = {
  ENVIRONMENT,
  ROLES,
};
